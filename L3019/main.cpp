#include <cstdio>
#include <cstdlib>
#include <cmath>
#include <cstring>
#include <algorithm>
using namespace std;

const int maxn=1e3+10;

char str[maxn];

int main()
{
    int len,g=-1;
    scanf("%s",str);
    len=strlen(str);
    for (i=0;i<len;i++)
    {

    }
    return 0;
}
